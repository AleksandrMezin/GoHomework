package main

/*
type calculator struct {
}
type Calculate(&calculator)float64,float64(string){

}

	func division(a, b float64) float64 {
		c := a / b
		return c
	}

	func multiply(a, b float64) float64 {
		c := a * b
		return c
	}

	func minus(a, b float64) float64 {
		c := a - b
		return c
	}

	func sum(a, b float64) float64 {
		c := a + b
		return c
	}
*/
package calc (calculator)

calculator := Calc{
minus : a - b,
sum : a + b,
division : a / b,
multiply : a * b,
}

type Calc struct {
	minus    float64
	sum      float64
	division float64
	multiply float64
}


func main() {

	/*	var a, b float64
		var c string
		fmt.Scan(&c)
		fmt.Println("enter operation : (-),(+),(*),(/)")
		fmt.Fscan(os.Stdin, &a, &b)

		switch  {
		c="-" : minus(a,b)
		c="+" : sum(a,b)
		c=`*` : multiply(a,b)
		c=`\` : division(a,b)
		if b==0 {
			fmt.Println("error")
			break
		}}*/

}
